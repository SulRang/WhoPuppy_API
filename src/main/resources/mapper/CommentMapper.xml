<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.whopuppy.mapper.CommentMapper">

    <sql id="commentColumns">
        board_idx
		, content
		, writer
    </sql>

    <insert id="insertComment">
        INSERT INTO whopuppy.animal_comment (board_idx, content, writer )
        VALUES (#{board_id}, #{content}, #{writer})
    </insert>

    <select id="selectCommentDetail" parameterType="long" resultType="com.whopuppy.domain.CommentDTO">
        SELECT
        *
        FROM
        animal_comment
        WHERE
        delete_yn = 'N'
        AND
        idx = #{comment_id}
    </select>

    <update id="updateComment" parameterType="com.whopuppy.domain.CommentDTO">
        UPDATE animal_comment
        SET
            update_time = NOW()
          , content = #{content}
          , writer = #{writer}
        WHERE
            idx = #{comment_id}
    </update>

    <update id="deleteComment" parameterType="long">
        UPDATE animal_comment
        SET
            delete_yn = 'Y'
          , delete_time = NOW()
        WHERE
            idx = #{comment_id}
    </update>

    <select id="selectCommentList" parameterType="long" resultType="com.whopuppy.domain.CommentDTO">
        SELECT
        <include refid="commentColumns" />
        FROM
        whopuppy.animal_comment
        WHERE
        delete_yn = 'N'
        AND
        board_idx = #{board_id}
        ORDER BY
        idx DESC,
        insert_time DESC
    </select>

    <select id="selectCommentTotalCount" parameterType="long" resultType="int">
        SELECT
            COUNT(*)
        FROM
            animal_comment
        WHERE
            delete_yn = 'N'
          AND
            board_idx = #{board_id}
    </select>

</mapper>